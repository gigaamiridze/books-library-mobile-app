import React from 'react';
import { ScrollView, Text } from 'react-native';

function Selection() {
  return (
    <ScrollView>
      <Text>Selection</Text>
    </ScrollView>
  )
}

export default Selection;
